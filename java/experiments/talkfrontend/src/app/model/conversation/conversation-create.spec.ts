import { ConversationCreate } from './conversation-create';

describe('ConversationCreate', () => {
  it('should create an instance', () => {
    expect(new ConversationCreate(1, 1)).toBeTruthy();
  });
});
