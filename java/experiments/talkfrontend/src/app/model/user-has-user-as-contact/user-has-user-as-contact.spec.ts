import { UserHasUserAsContact } from './user-has-user-as-contact';

describe('UserHasUserAsContact', () => {
  it('should create an instance', () => {
    expect(new UserHasUserAsContact(1, 1)).toBeTruthy();
  });
});
